# Spec差分チェック（未実装差分一覧）- 2026-03-14

対象は `documents/spec/*.md` と現行実装（`apps/web`, `packages/shared`, `apps/desktop`）。

## 0. 判定方針

仕様書は、現行実装の調査結果と「本来こうしたい」要件を章立てで整理したものとして扱う。  
本資料の判定では、仕様要件（`documents/spec/*.md`）を基準とし、現行実装との差分を記録する。  
根拠: `documents/spec/00_目次.md`。

本資料の記載状態は次の3種類とする。

- 未実装
- 部分実装
- 判定保留

各項目は次の固定フォーマットで記載する。  
`項目名 / 判定 / 仕様要件 / 現実装 / 未達内容 / 根拠（Spec行・実装行）`

## 1. 未実装差分一覧

### M-01 会話スケジューラのサーバーサイド移行（1時間間隔）
項目名: 会話スケジューラのサーバーサイド移行（1時間間隔）  
判定: 未実装  
仕様要件: サーバーサイドで1時間間隔の自動生成へ移行する。  
現実装: クライアント側 Provider でスケジューラを起動し、15分間隔（±20%）と `localStorage` ロックで運用。  
未達内容: サーバー側定期実行基盤と1時間間隔運用が未実装。  
根拠（Spec行・実装行）: `documents/spec/04_会話生成系.md:19` / `apps/web/components/providers/ConversationSchedulerProvider.tsx:18-20`, `apps/web/components/providers/ConversationSchedulerProvider.tsx:75-79`, `apps/web/lib/scheduler/conversation-scheduler.ts:137`, `apps/web/lib/scheduler/conversation-scheduler.ts:141`, `apps/web/lib/scheduler/conversation-scheduler.ts:268`

### M-02 シチュエーション生成のGPT化
項目名: シチュエーション生成のGPT化  
判定: 未実装  
仕様要件: 固定マッピングを廃止し、gpt-5-miniで毎回シチュエーションを生成する。  
現実装: 時間帯ごとの固定場所（駅前カフェ/商店街/川沿い公園/コンビニ）を返す。  
未達内容: LLM生成・重複回避・日付入力を使うシチュエーション生成が未実装。  
根拠（Spec行・実装行）: `documents/spec/04_会話生成系.md:44-53` / `apps/web/lib/conversation/run-conversation.ts:578-590`

### M-03 話題選定エンジン（9ソース + 重み付きランダム）
項目名: 話題選定エンジン（9ソース + 重み付きランダム）  
判定: 未実装  
仕様要件: 9種ソースを候補化し、重み付きランダムで1話題を選定する。  
現実装: 型定義が旧7ソースのままで、選定は最高スコア群からのランダム。  
未達内容: 9ソース化と重み付き抽選が未実装。  
根拠（Spec行・実装行）: `documents/spec/04_会話生成系.md:61-83` / `packages/shared/types/conversation-generation.ts:73-81`, `packages/shared/logic/topic-selection.ts:298-306`, `packages/shared/logic/topic-selection.ts:314-317`

### M-04 会話構造決定（favor減衰 + 25%ニュートラルノイズ）
項目名: 会話構造決定（favor減衰 + 25%ニュートラルノイズ）  
判定: 未実装  
仕様要件: 話題関心度に応じてfavor寄与を減衰し、25%でニュートラル方向に1段階シフトする。  
現実装: `topicInterest` と `favorScore` を直接加点し、確率ノイズ処理はない。  
未達内容: favor減衰ロジックと日常ノイズロジックが未実装。  
根拠（Spec行・実装行）: `documents/spec/04_会話生成系.md:118-141` / `packages/shared/logic/conversation-structure.ts:103-146`, `packages/shared/logic/conversation-structure.ts:154-165`

### M-05 一発話長制御とバリデーション閾値
項目名: 一発話長制御とバリデーション閾値  
判定: 部分実装  
仕様要件: 20〜30文字目安、40文字上限、`enthusiastic_short=12`、`indifferent_long=30`、`over_length=40` を適用する。  
現実装: プロンプトは「短文2〜3文程度」中心で、validator閾値は `enthusiastic<8`、`indifferent>20`。`over_length` 警告ルールも見当たらない。  
未達内容: 仕様閾値への整合と `heuristic_over_length` 相当チェックが未実装。  
根拠（Spec行・実装行）: `documents/spec/04_会話生成系.md:182-199`, `documents/spec/04_会話生成系.md:317` / `packages/shared/gpt/prompts/conversation-prompt.ts:370-373`, `packages/shared/logic/conversation-validator.ts:237`, `packages/shared/logic/conversation-validator.ts:267`, `packages/shared/logic/conversation-validator.ts:83`

### M-06 会話設定への日付情報追加
項目名: 会話設定への日付情報追加  
判定: 未実装  
仕様要件: 会話設定に日付情報を追加する。  
現実装: `PromptInput.environment` は `place/timeOfDay/weather` までで、会話設定出力にも日付がない。  
未達内容: 日付の入力型・プロンプト注入が未実装。  
根拠（Spec行・実装行）: `documents/spec/04_会話生成系.md:310-312` / `packages/shared/gpt/prompts/conversation-prompt.ts:47`, `packages/shared/gpt/prompts/conversation-prompt.ts:320-333`

### M-07 呼び方（ニックネーム）の自動生成・注入
項目名: 呼び方（ニックネーム）の自動生成・注入  
判定: 部分実装  
仕様要件: 関係遷移時の自動生成、ロック、傾向プリセット、会話プロンプト注入を行う。  
現実装: Officeでの手動登録CRUDはあるが、会話生成への注入導線はない。  
未達内容: 自動生成、ロック運用、会話プロンプト反映が未実装。  
根拠（Spec行・実装行）: `documents/spec/02_キャラクター定義系.md:169-177` / `apps/web/lib/data/residents.ts:152-237`, `apps/web/lib/conversation/run-conversation.ts:656-665`

### M-08 関係ラベル遷移イベント（観察型・介入型）
項目名: 関係ラベル遷移イベント（観察型・介入型）  
判定: 未実装  
仕様要件: `none→acquaintance`、`friend→best_friend`、告白/別れ相談などの遷移フローを実装する。  
現実装: 関係データは手動CRUDで編集可能だが、遷移イベント生成・自動遷移処理は確認できない。  
未達内容: 仕様で定義された遷移条件とイベント駆動更新が未実装。  
根拠（Spec行・実装行）: `documents/spec/03_関係・感情系.md:405-497` / `apps/web/lib/data/relations.ts:26-52`, `apps/web/lib/data/residents.ts:152-169`, `apps/web/lib/persist/persist-conversation.ts:13-102`

### M-09 相談イベント発生ロジック（日常相談・告白相談・別れ相談）
項目名: 相談イベント発生ロジック（日常相談・告白相談・別れ相談）  
判定: 未実装  
仕様要件: 信頼度や関係条件に応じて相談イベントを生成し、プレイヤー介入へ接続する。  
現実装: 相談は取得/回答処理が中心で、発生生成の本実装導線は確認できない。  
未達内容: 相談の定期発生・条件判定・生成パイプラインが未実装。  
根拠（Spec行・実装行）: `documents/spec/06_プレイヤー介入・インフラ系.md:56-116`, `documents/spec/03_関係・感情系.md:444-488` / `apps/web/lib/scheduler/conversation-scheduler.ts:331-399`, `apps/web/lib/conversation/run-conversation.ts:775-810`, `apps/web/app/api/consults/[id]/route.ts:71-105`, `apps/web/lib/client/consult-storage.ts:238-266`

### M-10 好感度変動の非対称化（感度 × スタンス × 話題補正）
項目名: 好感度変動の非対称化（感度 × スタンス × 話題補正）  
判定: 未実装  
仕様要件: A→B/B→Aで `sensitivity × stanceModifier × topicBonus` を掛けて非対称化する。  
現実装: タグ・品質・バランス加点を双方向同値で適用している。  
未達内容: 方向別補正係数による非対称計算が未実装。  
根拠（Spec行・実装行）: `documents/spec/03_関係・感情系.md:190-265` / `apps/web/lib/evaluation/evaluate-conversation.ts:105-132`

### M-11 印象変化の直近3件重み付き評価
項目名: 印象変化の直近3件重み付き評価  
判定: 未実装  
仕様要件: 直近3件のdeltaを重み `[4,3,2]` で評価し、閾値±7で1段階変化させる。  
現実装: 単会話内のfavor符号で即時1段階変化させる。評価入力にも履歴窓がない。  
未達内容: 3件蓄積評価ロジックと閾値判定が未実装。  
根拠（Spec行・実装行）: `documents/spec/03_関係・感情系.md:81-97` / `apps/web/lib/evaluation/evaluate-conversation.ts:151-154`, `apps/web/lib/conversation/run-conversation.ts:795-803`

### M-12 時間経過による印象・好感度の自然変化
項目名: 時間経過による印象・好感度の自然変化  
判定: 未実装  
仕様要件: 最終接触からの日数に応じて印象と好感度を自然変化させる。  
現実装: 会話時のdelta反映はあるが、日数経過ベースの自然変化処理は確認できない。  
未達内容: 時間経過トリガーと自然変化アルゴリズムが未実装。  
根拠（Spec行・実装行）: `documents/spec/03_関係・感情系.md:108-129`, `documents/spec/03_関係・感情系.md:267-275` / `apps/web/lib/evaluation/evaluate-conversation.ts:98-229`, `apps/web/lib/persist/persist-conversation.ts:44-46`

### M-13 `trustToPlayer` 初期値算出式
項目名: `trustToPlayer` 初期値算出式  
判定: 未実装  
仕様要件: 性格5軸・MBTI補正・ジッターで初期信頼度を算出する。  
現実装: 正規化関数は値の丸め/クランプのみで、既定値50を基準にしている。  
未達内容: 仕様式による初期算出ロジックが未実装。  
根拠（Spec行・実装行）: `documents/spec/03_関係・感情系.md:337-385` / `apps/web/lib/data/residents.ts:16-24`, `apps/web/components/forms/resident-form.tsx:284`

### M-14 `relation = none` 会話不可の厳密運用
項目名: `relation = none` 会話不可の厳密運用  
判定: 部分実装  
仕様要件: `relation = none` ペアは候補選定とパイプライン双方で会話対象外にする。  
現実装: 明示的な `none` レコードがある場合は除外するが、関係未取得時には `acquaintance` へフォールバックする。  
未達内容: 関係未定義ケースを含む厳密な会話不可制御が未実装。  
根拠（Spec行・実装行）: `documents/spec/03_関係・感情系.md:24` / `apps/web/lib/scheduler/conversation-scheduler.ts:70-84`, `apps/web/lib/conversation/run-conversation.ts:780`

### M-15 住人詳細「最近の変化」タブ
項目名: 住人詳細「最近の変化」タブ  
判定: 未実装  
仕様要件: 関係/印象/信頼度の変化を時系列で表示する。  
現実装: プレースホルダ文言のみ。  
未達内容: 変化イベント収集と時系列描画が未実装。  
根拠（Spec行・実装行）: `documents/spec/07_UI・画面設計.md:46` / `apps/web/app/(dashboard)/office/residents/[id]/page.tsx:356-364`

### M-16 覗く機能（LLM生成）
項目名: 覗く機能（LLM生成）  
判定: 未実装  
仕様要件: 住人の「今の様子 + 独り言」をLLM生成して表示する。  
現実装: UIは準備中文言のみ。  
未達内容: LLM生成導線と表示データ構築が未実装。  
根拠（Spec行・実装行）: `documents/spec/06_プレイヤー介入・インフラ系.md:120-129` / `apps/web/components/room/home-content.tsx:999-1001`

### M-17 旧experience系テーブルの廃止
項目名: 旧experience系テーブルの廃止  
判定: 未実装  
仕様要件: `experienceEvents` / `residentExperiences` を削除対象として扱う。  
現実装: DrizzleスキーマとKV対象に旧テーブルが残存。  
未達内容: 旧テーブル除去と参照整理が未実装。  
根拠（Spec行・実装行）: `documents/spec/05_世界シミュレーション系.md:231-235`, `documents/spec/00_目次.md:38` / `apps/web/lib/drizzle/schema.ts:202-237`, `apps/web/lib/db/kv-server.ts:22-35`, `apps/web/lib/db/kv-client.ts:7-15`

### M-18 天気コメント生成モデルの仕様整合
項目名: 天気コメント生成モデルの仕様整合  
判定: 部分実装  
仕様要件: 天気コメントは gpt-5-mini で生成する。  
現実装: 天気コメント生成は `gpt-5-mini` で呼び出している。  
未達内容: モデル指定が仕様と不一致。  
根拠（Spec行・実装行）: `documents/spec/05_世界シミュレーション系.md:89` / `apps/web/lib/gpt/call-gpt-for-weather-comment.ts:66`

### M-19 最近の出来事の件数・文字数制約（0〜1件/人・20文字以内）
項目名: 最近の出来事の件数・文字数制約（0〜1件/人・20文字以内）  
判定: 部分実装  
仕様要件: 生成件数は 0〜1件/人、文字数は20文字以内。  
現実装: プロンプト上の指示はあるが、保存前に「1人1件制約」「20文字制約」をコードで検証していない。  
未達内容: 出力後バリデーション（重複characterIdの抑制、fact長の制限）が未実装。  
根拠（Spec行・実装行）: `documents/spec/05_世界シミュレーション系.md:159-160` / `packages/shared/gpt/prompts/recent-events-prompt.ts:31-35`, `apps/web/lib/batch/generate-recent-events.ts:162-170`, `apps/web/lib/batch/generate-recent-events.ts:172-180`

### M-20 通知スニペットの60文字切り出し仕様
項目名: 通知スニペットの60文字切り出し仕様  
判定: 部分実装  
仕様要件: 通知生成時の `snippet` は会話1行目を60文字に切り出す。  
現実装: 会話永続化経路では `speaker + 先頭28文字 + …` を保存している。  
未達内容: メイン生成経路の `snippet` 生成が60文字仕様と不一致。  
根拠（Spec行・実装行）: `documents/spec/06_プレイヤー介入・インフラ系.md:41` / `apps/web/lib/persist/persist-conversation.ts:223-226`

### M-21 Outboxパターンの全変更適用
項目名: Outboxパターンの全変更適用  
判定: 部分実装  
仕様要件: ローカル変更をOutboxにキューし、オンライン復帰時に送信する。  
現実装: 同期本体は `since` 差分でローカル直接変更を送る方式が中心で、Outboxエントリ投入は `consult_answers` の一部フォールバック時のみ。  
未達内容: 住人・関係・感情など通常更新経路でOutboxへのキュー投入が行われておらず、全変更をOutboxで一元管理する仕様になっていない。  
根拠（Spec行・実装行）: `documents/spec/06_プレイヤー介入・インフラ系.md:164` / `apps/web/lib/sync/use-sync.tsx:111-116`, `apps/web/lib/client/consult-storage.ts:174-186`, `apps/web/lib/data/residents.ts:272`

### M-22 印象の2系列運用（通常系列/特別系列）と `love` 段階
項目名: 印象の2系列運用（通常系列/特別系列）と `love` 段階  
判定: 未実装  
仕様要件: 関係に応じて通常系列/特別系列を切り替え、特別系列では `like -> love` を含む段階運用を行う。  
現実装: 会話評価は単一の印象ラダーで処理し、評価用の `ImpressionBase` に `love` が存在しない。評価入力にも関係系列切替に必要な関係種別がない。  
未達内容: 関係ラベル連動の系列切替、昇降格時の系列変換、特別系列の `love` を含む段階評価が未実装。  
根拠（Spec行・実装行）: `documents/spec/03_関係・感情系.md:43-75` / `packages/shared/types/conversation.ts:9`, `apps/web/lib/evaluation/evaluate-conversation.ts:16-32`, `apps/web/lib/evaluation/weights.ts:52`, `apps/web/lib/persist/persist-conversation.ts:25-34`

### M-23 好感度の中立点30・関係別初期値テーブル
項目名: 好感度の中立点30・関係別初期値テーブル  
判定: 未実装  
仕様要件: 好感度は中立点30を基準とし、関係ラベルごとの初期値テーブル（none=30, acquaintance=35, friend=45, ...）で初期提案する。  
現実装: feelings の既定値は0、住人フォームの関係編集既定値は50で、関係タイプ変更時に初期値テーブルを自動適用する処理もない。  
未達内容: 仕様の中立点30基準および関係別初期値テーブル運用が反映されていない。  
根拠（Spec行・実装行）: `documents/spec/03_関係・感情系.md:171-186` / `apps/web/lib/drizzle/schema.ts:104`, `packages/shared/types/index.ts:89`, `apps/web/components/forms/resident-form.tsx:133-135`, `apps/web/components/forms/resident-form.tsx:363-377`, `apps/web/components/forms/resident-form.tsx:436-439`

### M-24 家族関係の種別情報（兄/姉/父/母など）
項目名: 家族関係の種別情報（兄/姉/父/母など）  
判定: 未実装  
仕様要件: `family` 関係には種別情報（兄/姉/父/母など）を保持する。  
現実装: relationは `type` のみを保持し、familyサブタイプを保持するカラム・型・フォーム入力がない。  
未達内容: 家族種別の保存・編集・表示導線が未実装。  
根拠（Spec行・実装行）: `documents/spec/03_関係・感情系.md:485-489` / `apps/web/lib/drizzle/schema.ts:82-86`, `packages/shared/types/index.ts:79-83`, `packages/shared/types/index.ts:192-203`

### M-25 通知タイプ `consult` / `system` の自動生成
項目名: 通知タイプ `consult` / `system` の自動生成  
判定: 部分実装  
仕様要件: 会話/相談/システムイベント作成時に通知を自動生成し、`type` は `conversation` / `consult` / `system` を使い分ける。  
現実装: 通常導線の通知生成は `conversation` 固定。`consult` 通知の生成は開発用 seed ページでのみ確認できる。  
未達内容: 通常導線での `consult` / `system` 通知自動生成が未実装。  
根拠（Spec行・実装行）: `documents/spec/06_プレイヤー介入・インフラ系.md:18-24`, `documents/spec/06_プレイヤー介入・インフラ系.md:41` / `apps/web/lib/persist/persist-conversation.ts:154`, `apps/web/lib/repos/conversation-repo.ts:225`, `apps/web/app/dev/seed-notifications/page.tsx:66`

### M-26 `consult` 通知クリック時の相談詳細遷移（`linkedEventId` 利用）
項目名: `consult` 通知クリック時の相談詳細遷移（`linkedEventId` 利用）  
判定: 部分実装  
仕様要件: `consult` 通知クリックで相談詳細を開く。  
現実装: `consult` 通知を開く際のID解決で `linkedConsultId` / `payload.consultId` / `consultId` のみ参照し、通知スキーマの `linkedEventId` を使っていない。  
未達内容: 標準通知スキーマ（`linkedEventId`）だけで `consult` 詳細を開ける導線が未実装。  
根拠（Spec行・実装行）: `documents/spec/06_プレイヤー介入・インフラ系.md:49` / `apps/web/lib/drizzle/schema.ts:243`, `apps/web/components/notifications/NotificationsPanel.tsx:82-85`, `apps/web/components/room/home-content.tsx:292-294`

### M-27 設定画面の同期ON/OFFの実効制御
項目名: 設定画面の同期ON/OFFの実効制御  
判定: 部分実装  
仕様要件: 設定画面のデータ管理で同期設定を管理できる。  
現実装: `syncEnabled` は設定UIとローカル保存はあるが、同期処理側で参照されず、同期実行の制御に使われていない。  
未達内容: 同期ON/OFFが実際の同期実行（自動/手動トリガ）へ反映される制御が未実装。  
根拠（Spec行・実装行）: `documents/spec/07_UI・画面設計.md:74` / `apps/web/app/(dashboard)/settings/page.tsx:68-69`, `apps/web/lib/use-settings.ts:11`, `apps/web/lib/use-settings.ts:17`, `apps/web/lib/sync/use-sync.tsx:88-100`, `apps/web/lib/sync/use-sync.tsx:220-280`

### M-28 Tauri環境でのOutboxストレージ切替
項目名: Tauri環境でのOutboxストレージ切替  
判定: 部分実装  
仕様要件: Tauri環境では IndexedDB の代わりにファイルシステムベースJSONストレージへ透過的に切り替える。  
現実装: メインローカルストレージはTauri向けJSON切替がある一方、Outboxは `indexedDB.open` 固定。  
未達内容: Outbox層のTauri向けストレージ切替が未実装。  
根拠（Spec行・実装行）: `documents/spec/06_プレイヤー介入・インフラ系.md:194-204` / `apps/web/lib/db-local/index.ts:64`, `apps/web/lib/db-local/index.ts:136-160`, `apps/web/lib/sync/outbox.ts:21-24`

## 2. 解消済み（過去バグ修正）

### R-01 `consult_answers` push無効バグの解消
項目名: `consult_answers` push無効バグの解消  
判定: 解消済み（過去バグ修正）  
仕様要件: 「push無効になっているバグ」を解消し、`consult_answers` をサーバーへ反映できるようにする。  
現実装: `consult_answers` は専用処理で insert（first-write-wins）される。  
未達内容: なし。  
根拠（Spec行・実装行）: `documents/spec/06_プレイヤー介入・インフラ系.md:112-116` / `apps/web/app/api/sync/[table]/route.ts:204-213`, `apps/web/app/api/sync/[table]/route.ts:277-285`

## 3. 判定保留（議論待ち）

### P-01 スレッド占有対策（`close` デフォルト運用）
項目名: スレッド占有対策（`close` デフォルト運用）  
判定: 判定保留  
仕様要件: `close` をデフォルトに寄せ、`continuation` 話題の占有を抑制する。  
現実装: `signals` と `threadNextState` は実装済みだが、`close` をデフォルト運用とする設計反映範囲は議論中。  
未達内容: 仕様確定後に再判定。  
根拠（Spec行・実装行）: `documents/spec/04_会話生成系.md:249-254` / `packages/shared/gpt/prompts/conversation-prompt.ts:71`, `apps/web/lib/evaluation/evaluate-conversation.ts:68-71`, `apps/web/lib/persist/persist-conversation.ts:117-129`

### P-02 `owner_id` ベースRLSのDBポリシー実体
項目名: `owner_id` ベースRLSのDBポリシー実体  
判定: 判定保留  
仕様要件: `owner_id` ベースのRLSでユーザー分離する。  
現実装: 同期APIで `owner_id` 強制注入と認証チェックは行っている一方、DBポリシー定義の管理場所はリポジトリ上で明示されていない。  
未達内容: 実運用DBのRLSポリシー定義がどこで担保されるか（コード内/DB設定）の確認が必要。  
根拠（Spec行・実装行）: `documents/spec/06_プレイヤー介入・インフラ系.md:166`, `documents/spec/06_プレイヤー介入・インフラ系.md:188` / `apps/web/app/api/sync/[table]/route.ts:255-262`, `apps/web/app/api/sync/[table]/route.ts:316-317`, `apps/web/lib/drizzle/db.ts:8-10`

## 4. 継続項目（未再確認）

今回、前回一覧の項目はすべて再判定済み。  
継続項目（未再確認）はなし。
