{
  "name": "reladen-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@8.15.4",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "lint": "pnpm --filter web lint",
    "build": "pnpm --filter web build",
    "test": "pnpm --filter web test",
    "db:push": "pnpm --filter web db:push",
    "seed": "pnpm --filter web seed",
    "tauri:dev": "pnpm --filter desktop tauri dev",
    "tauri:build": "pnpm --filter desktop tauri build",
    "tsx": "apps/web/node_modules/.bin/tsx",
    "seed:min": "tsx scripts/seed_min.ts"
  },
  "devDependencies": {
    "tsx": "^4.7.1",
    "turbo": "^1.13.3"
  },
  "dependencies": {
    "zod": "^3.25.76"
  }
}
