# 1-6 前提・制約
- GPT-APIアクセスとSupabase(Postgres)接続が可能であることを前提とする。
- tombstone(`deleted`/`updated_at`)運用を前提に、RLS(owner_id)でユーザーデータを分離する。
- WebではIndexedDB、デスクトップではTauri app.dbでローカル保持し、オンライン時に同期する。
- 通信はHTTPS前提。環境変数(.env)で鍵を管理し、クライアントに秘匿情報を埋め込まない。
