# 2-1 全体構成
ReladenはWeb(PWA, Next.js App Router)とデスクトップ(Tauri)の2クライアントで構成され、どちらもローカルDB(IndexedDB / Tauri app.db)にデータを保持しつつ、オンライン時にSupabase(Postgres)と同期する。GPT-APIを会話生成・WorldFacts生成に利用し、サーバーレスAPI(`/app/api/sync/{table}`など)経由でバックエンドとやりとりする。PWAはオフラインキャッシュと通知表示を担い、Tauriはネイティブ配布と安定したローカル保存領域を提供する。
